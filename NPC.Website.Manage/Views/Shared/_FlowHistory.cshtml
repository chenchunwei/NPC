@using NPC.Domain.Models.FlowNodeInstances
@model NPC.Domain.Models.Flows.Flow
<div class="main_search">
    <div class="mc_search">
        <div class="pic">
            <img src="@Url.Content("~/Content/images/icon_12.gif")" alt="" /></div>
        <div class="font">
            操作历史记录：</div>
    </div>
</div>
<div class="row">
    <style type="text/css">
        .fromTable tr.header
        {
            margin: 0 10px;
            overflow: hidden;
            color: #666666;
            border-bottom: 1px solid #DBE2EC;
            border-top: 1px solid #DBE2EC;
            background: #E6EAF3;
        }
        .fromTable td
        {
            padding: 2px 10px;
            border-right: 1px solid #DBE2EC;
        }
        .fromTable tr
        {
            height: 22px;
            line-height: 22px;
            border-bottom: 1px solid #DBE2EC;
        }
        .fromTable tr.footer
        {
            height: 5px;
            line-height: 5px;
            border-bottom: 1px solid #DBE2EC;
        }
    </style>
    <div class="treeBody" style="padding-top: 5px;">
        <table class="fromTable " width="100%">
            <tr class="header">
                <td style="width: 22px;">
                    <img src="@Url.Content("~/Content/Images/flag-16-16-red.gif")" alt=""/>
                </td>
                <td style="width: 230px;">
                    阶段名称
                </td>
                <td style="width: 130px;">
                    操作时间
                </td>
                <td style="width: 150px;">
                    操作人
                </td>
                <td style="width: 230px;">
                    操作动作
                </td>
                <td>
                    备注
                </td>
            </tr>
            @foreach (var record in Model.FlowHistories)
            {
                <tr>
                    <td>
                        <img src="@Url.Content("~/Content/Images/hook-12-12-green.gif")" alt=""/>
                    </td>
                    <td>
                        @record.Stage
                    </td>
                    <td>
                        @record.RecordDescription.DateOfCreate.ToString("yyyy-MM-dd HH:mm")
                    </td>
                    <td>
                        @(record.RecordDescription.UserOfCreate != null ? record.RecordDescription.UserOfCreate.Name : "")
                    </td>
                    <td>
                        <b style="color: #c30">@record.Operator</b>
                    </td>
                    <td>
                        @Html.Raw(record.Comment)
                    </td>
                </tr>
            }
            @foreach (var task in Model.FlowNodeInstances.Where(o => o.InstanceStatus == InstanceStatus.Runing))
            {
                foreach (var user in task.FlowNodeInstanceUserStates)
                {
                <tr>
                    <td>
                    </td>
                    <td>
                        @task.BelongsFlowNode.Name
                    </td>
                    <td>
                        &nbsp;
                    </td>
                    <td>
                        @user.User.Name
                    </td>
                    <td>
                        &nbsp;
                    </td>
                    <td>
                        &nbsp;
                    </td>
                </tr>
                }
            }
            <tr class="footer">
                <td colspan="6">
                    &nbsp;
                </td>
            </tr>
        </table>
    </div>
    <div class="footBar">
        <div class="fb_content">
        </div>
    </div>
</div>
